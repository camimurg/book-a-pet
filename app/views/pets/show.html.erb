
    <h1> <%= @pet.name %> 🦕 </h1>
    <p> <%= @pet.species %> </p>
    <p> <%= @pet.address %> </p>
    <p> <%= @pet.city %> </p>
    <p> <%= @pet.description %> </p>

    <% if @pet.picture.attached? %>
      <%= cl_image_tag @pet.picture.key, height: 300, width: 400, crop: :fill %>
    <% end  %>



  <%= simple_form_for([@pet, @appointment]) do |f| %>
    <%= f.input :date %>
    <%= f.input :start_time, as: :time, :minute_step => 15 %>
    <%= f.input :end_time, as: :time, :minute_step => 15 %>
    <%= f.input :message %>
    <%= f.submit class: "btn btn-primary" %>
  <% end %>


  <%= link_to "Back to all our pets", pets_path %>

