
<h1>All our pets</h1>

<% @pets.each do |pet| %>
  <h3> <%= link_to pet.name, pet_path(pet) %> </h3>
  <p> <%= pet.species %> </p>
  <p> <%= pet.city %> </p>
<% end %>


<%= link_to "Add your pet", new_pet_path %>
